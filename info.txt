#program
Set and unset aliases

# Change directories
~/termy/data (storage)
command: r [partial name / fuzzy find]

# Commands (aliases)
set when change of directory with termy
command: /r\w/
add last executed command: ra

# Managing commands
Add commands by choice of last x history commands

# Dynamic folder aliases
load file when dir pattern matches
[pattern] [path to rr file]


# Frecently algorithm
- score and time system
- score = score + (now - score) / divider // divider = 5, and gets temporary -1 when used

item data
- last_used_time
- score
when used
- score multiplier within ranges?
if score < 300 && last_used_time < 1 hour
	score += 100
else
	score += 20
- last_used_time updated to now

when loaded
- score updated
- score = score * (0.99 ** item.last_used_time - time.now)
